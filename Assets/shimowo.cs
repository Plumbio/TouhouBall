using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class shimowo : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("ewé");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
